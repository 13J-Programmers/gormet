<%= form_with(model: food, local: true) do |form| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :menu_id %>
    <%= form.number_field :menu_id, id: :food_menu_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.number_field :order_id, id: :food_order_id %>
  </div>

  <div class="field">
    <%= form.label :with_cheese %>
    <%= form.check_box :with_cheese, id: :food_with_cheese %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
